import { createRoot } from "react-dom/client";
// import App from "./App.tsx"; // Temporarily removed
import "./globals.css";

createRoot(document.getElementById("root")!).render(
  <div>Hello from main.tsx!</div>
);